import currentCountryAction from './currentCountryAction'
import currentCityAction from './currentCityAction'
import currentHotelAction from './currentHotelAction'

const allActions = {
    currentCountryAction, currentCityAction, currentHotelAction
}

export default allActions;